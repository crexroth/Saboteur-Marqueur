const config = {
    roles: [
        { value: "Miner", label: "Miner" },
        { value: "Troll", label: "Troll" },
        { value: "Observer", label: "Observer" }
    ],
    maxPlayers: 10,
    minPlayers: 2
};

export default config;
